# visual (generated)

> Generated from `docs/proposal.md`. Agents: keep up to date with code.

Purpose: define the shell's overall visual style.

Owns: palette, type, density, component look.
Owns not: page layouts (see `pages`), Tailwind setup (see `structure`).

Locked:
- Tailwind under the hood, but the look must be a custom identity — never the obvious default Tailwind/LLM style (default palette, gradient hero, generic cards everywhere).

Open: palette, typography, density, signature elements.
