> AGENTS should never update this doc, unless explicitly asked

Just like vscode ssh into a dev server, but through browser -- so accesible from anywhere

Goals
- Tech
    - Deploy with AWS EC2
    - Minimum backend resource requirement
    - Use cloud platform features, but keep independent (open to switch) from specific platform
- Feature
    - Sync and manage projects -- premium user can save as files in server; normal user can use public repo
    - Premium user can access and use server (that used for deployment) shell, just like ssh; normal user cannot

Non-Goals
- Vscode extensions support
